"use client";

/**
 * TrackingListener — anteriormente rastreava cliques de WhatsApp como conversão.
 * Com o funil 100% Site + Stripe, não há mais conversão de WhatsApp a rastrear.
 * Mantido como componente vazio para não quebrar o layout.tsx.
 */
export function TrackingListener() {
  return null;
}
